const initStete = {
  city: null,
  country: null,
  icon: null,
  main: null,
  celsius: null,
  description: "",
  error: false,
};

export default (state = initStete, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
};
